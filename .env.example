# =============================================================================
# ENVIRONMENT VARIABLES CONFIGURATION
# =============================================================================
# Copy this file to .env.local (for local development) and set actual values.
# For production deployment, configure these in Vercel Project Settings.
#
# See DEPLOYMENT.md for detailed setup instructions for each service.
# See docs/recipes/env-setup.md for environment variable patterns.
# =============================================================================

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# App Name (displayed in UI, emails, etc.)
# Required: Yes
NEXT_PUBLIC_APP_NAME=DL Starter

# App URL (used for OAuth callbacks, email links, etc.)
# Required: Yes (in production)
# Example: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node Environment (automatically set by hosting platform)
# Values: development | test | production
NODE_ENV=development

# =============================================================================
# DATABASE (SUPABASE)
# =============================================================================
# Setup: https://supabase.com/dashboard → Create Project
# Docs: DEPLOYMENT.md#database-setup-supabase

# Database Connection String (PostgreSQL)
# Required: Yes (for database features)
# Location: Supabase Dashboard → Project Settings → Database → Connection String (URI mode)
DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres

# Supabase Project URL (public)
# Required: Yes (for auth and database features)
# Location: Supabase Dashboard → Project Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (public, safe to expose)
# Required: Yes (for auth and database features)
# Location: Supabase Dashboard → Project Settings → API → Project API keys → anon/public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (secret, server-only)
# Required: Yes (for admin operations, migrations, seeds)
# Location: Supabase Dashboard → Project Settings → API → Project API keys → service_role
# ⚠️ WARNING: Never expose this key to the client! Server-side only.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Supabase Access Token (for CLI and MCP tooling)
# Required: No (only for CLI operations)
# Location: Supabase Dashboard → Account → Access Tokens
SUPABASE_ACCESS_TOKEN=

# Supabase Project Reference (for CLI operations)
# Required: No (only for CLI operations)
# Location: Supabase Dashboard → Project Settings → General → Reference ID
SUPABASE_PROJECT_REF=

# =============================================================================
# AUTHENTICATION
# =============================================================================
# Docs: DEPLOYMENT.md#authentication-setup

# Enable authentication features
# Required: No (default: false)
# Values: true | false | 1 | 0
AUTH_ENABLED=true

# Authentication provider
# Required: No (default: supabase)
# Values: supabase (only supported provider currently)
AUTH_PROVIDER=supabase

# =============================================================================
# PAYMENTS (STRIPE)
# =============================================================================
# Setup: https://dashboard.stripe.com/ → API Keys
# Docs: DEPLOYMENT.md#payment-processing-stripe

# Stripe Publishable Key (public, client-side)
# Required: No (only if using Stripe)
# Location: Stripe Dashboard → Developers → API Keys → Publishable key
# Note: Use pk_test_... for testing, pk_live_... for production
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe Secret Key (secret, server-only)
# Required: No (only if using Stripe)
# Location: Stripe Dashboard → Developers → API Keys → Secret key
# Note: Use sk_test_... for testing, sk_live_... for production
# ⚠️ WARNING: Never expose this key to the client! Server-side only.
STRIPE_SECRET_KEY=sk_test_...

# Stripe Webhook Secret (for webhook signature verification)
# Required: No (only if using Stripe webhooks)
# Location: Stripe Dashboard → Developers → Webhooks → [Your webhook] → Signing secret
# Setup: Add webhook endpoint: https://your-app.vercel.app/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_...

# =============================================================================
# EMAIL SERVICE (RESEND)
# =============================================================================
# Setup: https://resend.com/overview → API Keys
# Docs: DEPLOYMENT.md#email-service-resend
# Note: This integration is planned (Issue #295)

# Resend API Key
# Required: No (only if using email features)
# Location: Resend Dashboard → API Keys
RESEND_API_KEY=re_...

# From Email Address (must be verified domain)
# Required: No (only if using email features)
# Setup: Resend Dashboard → Domains → Add Domain → Verify DNS
# Example: noreply@yourdomain.com or onboarding@resend.dev (for testing)
RESEND_FROM_EMAIL=noreply@yourdomain.com

# =============================================================================
# AI PROVIDERS
# =============================================================================
# Docs: DEPLOYMENT.md#ai-provider-setup
# Note: This integration is planned (Issue #294)

# Anthropic API Key (for Claude AI models)
# Required: No (only if using Anthropic/Claude features)
# Setup: https://console.anthropic.com/ → Get API Key
# Models: claude-3-5-sonnet-20241022, claude-3-5-haiku-20241022
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API Key (for GPT models)
# Required: No (only if using OpenAI/GPT features)
# Setup: https://platform.openai.com/api-keys → Create new secret key
# Models: gpt-4, gpt-3.5-turbo
OPENAI_API_KEY=sk-...

# =============================================================================
# INTERNATIONALIZATION (i18n)
# =============================================================================
# Docs: DEPLOYMENT.md#internationalization-i18n
# Note: This integration is planned (Issue #296)

# Default locale (used when user preference is not set)
# Required: No (only when i18n is implemented)
# Example: en, es, fr, de
NEXT_PUBLIC_DEFAULT_LOCALE=en

# Supported locales (comma-separated list)
# Required: No (only when i18n is implemented)
# Example: en,es,fr,de
NEXT_PUBLIC_SUPPORTED_LOCALES=en

# =============================================================================
# MONITORING & ANALYTICS
# =============================================================================

# ----------------------------------------------------
# Sentry (Error Tracking)
# ----------------------------------------------------
# Setup: https://sentry.io/ → Create Project
# Docs: DEPLOYMENT.md#monitoring-sentry
# Note: Sentry is OPTIONAL - app works without it

# Sentry DSN (server-side error tracking)
# Required: No (error tracking is optional)
# Location: Sentry Dashboard → Project Settings → Client Keys (DSN)
SENTRY_DSN=https://...@sentry.io/...

# Sentry DSN (client-side error tracking, usually same as SENTRY_DSN)
# Required: No (error tracking is optional)
NEXT_PUBLIC_SENTRY_DSN=https://...@sentry.io/...

# Sentry Organization Slug
# Required: No (only if using Sentry)
# Location: Sentry Dashboard → Settings → Organization Settings → Organization Slug
SENTRY_ORG=your-org-slug

# Sentry Project Slug
# Required: No (only if using Sentry)
# Location: Sentry Dashboard → Project Settings → General Settings → Project Slug
SENTRY_PROJECT=your-project-slug

# Sentry Auth Token (for source map uploads during build)
# Required: No (only if using Sentry)
# Location: Sentry Dashboard → Settings → Auth Tokens → Create New Token
# Scopes: project:read, project:releases, org:read
SENTRY_AUTH_TOKEN=sntrys_...

# ----------------------------------------------------
# PostHog (Product Analytics)
# ----------------------------------------------------
# Setup: https://posthog.com/ → Create Project
# Note: PostHog integration is optional

# PostHog API Key
# Required: No (analytics is optional)
# Location: PostHog Dashboard → Project Settings → Project API Key
NEXT_PUBLIC_POSTHOG_KEY=phc_...

# ----------------------------------------------------
# Feature Flags
# ----------------------------------------------------

# Enable analytics tracking (PostHog, Google Analytics, etc.)
# Required: No (default: false)
# Values: true | false
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Enable beta features (for testing new features)
# Required: No (default: false)
# Values: true | false
NEXT_PUBLIC_FLAG_BETA_FEATURE=false

# =============================================================================
# DEVELOPMENT & TESTING
# =============================================================================

# Bypass SSL verification (for local development with self-signed certs)
# Required: No (default: false)
# ⚠️ WARNING: Never set to true in production!
NODE_TLS_REJECT_UNAUTHORIZED=

# =============================================================================
# NOTES
# =============================================================================
#
# Environment Variable Naming Conventions:
# - NEXT_PUBLIC_* = Client-side variables (exposed to browser)
# - All others = Server-side only (never exposed to browser)
#
# Production Checklist:
# - ✅ Never use test API keys (sk_test_, pk_test_) in production
# - ✅ Set NODE_ENV=production (Vercel does this automatically)
# - ✅ Use strong, unique values for all secret keys
# - ✅ Enable Sentry for error tracking (recommended)
# - ✅ Verify all required variables are set in Vercel dashboard
#
# For detailed setup instructions, see:
# - DEPLOYMENT.md - Complete deployment guide
# - docs/recipes/env-setup.md - Environment variable patterns
# - apps/web/src/lib/env.ts - Validation rules (Zod schemas)
#
# =============================================================================