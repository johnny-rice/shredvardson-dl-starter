/**
 * DL Starter Tailwind Configuration
 *
 * Re-exports the shared Tailwind preset for use across the monorepo.
 *
 * @example
 * ```typescript
 * import { dlStarterPreset } from '@dl-starter/tailwind-config';
 *
 * const config: Config = {
 *   presets: [dlStarterPreset],
 *   content: ['./src/**\/*.{ts,tsx}'],
 * };
 * ```
 */
export { dlStarterPreset } from './src/preset';
