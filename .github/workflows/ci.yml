name: CI
on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]
  push:
    branches: [main]
  repository_dispatch:
    types: [doctor-recheck]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  setup:
    uses: ./.github/workflows/_setup-pnpm.yml

  doctor:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Detect Infrastructure Changes
        run: bash scripts/detect-infra-change.sh

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.12.0
          run_install: false

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create artifacts directory
        run: mkdir -p artifacts

      - name: Init doctor report
        run: echo "# Doctor Report" > artifacts/doctor-report.md

      - name: Run doctor
        env:
          DOCTOR_SILENT: 1
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_PR_NUMBER: ${{ github.event.pull_request.number || github.event.client_payload.pr_number }}
        run: pnpm run doctor:report

      - name: Audit large files (blocking)
        run: pnpm audit:large

      - name: Audit traceability (if traceability dirs exist)
        run: |
          set -euo pipefail
          if [ -d "specs" ] || [ -d "plans" ] || [ -d "tasks" ]; then
            echo "📋 Traceability artifacts detected, running validation..."
            # Ensure tsx is available after dependency installation
            pnpm audit:traceability || pnpm dlx tsx scripts/validate-traceability.ts
          else
            echo "📋 No traceability artifacts found, skipping validation"
          fi

      - name: Validate Database Changes
        run: |
          set -euo pipefail
          echo "🗄️  Checking for database changes..."
          git fetch origin main:refs/remotes/origin/main || true
          if git diff --name-only origin/main...HEAD | grep -E "(supabase/|packages/db/)" >/dev/null 2>&1; then
            echo "📊 Database changes detected, running validation..."
            pnpm dlx tsx scripts/db/ci-validate.ts
          else
            echo "📊 No database changes detected, skipping validation"
          fi

      - name: Scrape AI Reviews from PR Comments
        if: always() && github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Scrape structured AI review comments and append to doctor report
          PR_NUMBER=${{ github.event.pull_request.number }}
          echo "Scraping AI review comments from PR #$PR_NUMBER..."

          # Get PR head SHA for validation
          PR_HEAD_SHA=${{ github.event.pull_request.head.sha }}
          echo "PR head SHA: $PR_HEAD_SHA"

          # Get AI review comment content with anti-poisoning validation
          AI_REVIEW_CONTENT=$(gh api repos/${{ github.repository }}/issues/$PR_NUMBER/comments 2>/dev/null \
            | jq -r --arg sha "$PR_HEAD_SHA" '.[]
              | select(.user.login=="github-actions[bot]")
              | select(.body|contains("<!-- ai-review:v1") and contains($sha))
              | .body' || true)

          # Get AI security review comment content with anti-poisoning validation
          AI_SEC_CONTENT=$(gh api repos/${{ github.repository }}/issues/$PR_NUMBER/comments 2>/dev/null \
            | jq -r --arg sha "$PR_HEAD_SHA" '.[]
              | select(.user.login=="github-actions[bot]")
              | select(.body|contains("<!-- ai-sec-review:v1") and contains($sha))
              | .body' || true)

          # Extract and append AI review content
          if [ -n "$AI_REVIEW_CONTENT" ]; then
            echo "Found AI review comment, extracting content..."
            echo "$AI_REVIEW_CONTENT" | sed -n '/<!-- ai-review:v1.*-->/,/<!-- \/ai-review:v1 -->/p' | \
              sed '1d;$d' >> artifacts/doctor-report.md
            echo "" >> artifacts/doctor-report.md
          fi

          # Extract and append AI security review content
          if [ -n "$AI_SEC_CONTENT" ]; then
            echo "Found AI security review comment, extracting content..."
            echo "$AI_SEC_CONTENT" | sed -n '/<!-- ai-sec-review:v1.*-->/,/<!-- \/ai-sec-review:v1 -->/p' | \
              sed '1d;$d' >> artifacts/doctor-report.md
            echo "" >> artifacts/doctor-report.md
          fi

          # Add separator if any AI content was found; otherwise append a literal fallback block
          if [ -n "$AI_REVIEW_CONTENT" ] || [ -n "$AI_SEC_CONTENT" ]; then
            {
              echo "---"
              echo "*AI reviews aggregated by comment-scrape pipeline*"
              echo ""
            } >> artifacts/doctor-report.md
          else
            # Graceful fallback - AI review not available but don't block CI
            {
              echo "## ℹ️ AI Review Status"
              echo ""
              echo "AI review not available for this PR (no matching comments found for the current head SHA)."
              echo ""
              echo "> Advisory AI reviews are optional and don't affect CI status. Trigger with \`@claude /review\` in a PR comment."
              echo ""
            } >> artifacts/doctor-report.md
          fi

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: doctor-report
          path: |
            artifacts/doctor-report.md
            artifacts/command-inventory.json
          if-no-files-found: warn

  docs-link-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.12.0
          run_install: false

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check docs structure and links
        run: pnpm tsx scripts/docs-check.ts

  spec-gate:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Detect Infrastructure Changes
        run: bash scripts/detect-infra-change.sh
      - name: Run spec gate
        if: env.IS_INFRA_CHANGE != 'true'
        run: bash scripts/ci/spec_gate.sh
      - name: Skip spec gate for infra changes
        if: env.IS_INFRA_CHANGE == 'true'
        run: |
          echo "🔧 Infrastructure-only PR: Skipping spec gate"
          echo "✅ Spec requirements not applicable for tooling/workflow changes"

  ci:
    needs: setup
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      TURBO_TELEMETRY_DISABLED: '1'
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Detect Infrastructure Changes
        run: bash scripts/detect-infra-change.sh

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.12.0
          run_install: false

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Preflight: placeholders + env
      - name: Preflight (placeholders & env)
        run: |
          # Build the token dynamically to avoid committing the literal
          PATTERN="{{"; PATTERN+="PLACEHOLDER"; PATTERN+="}}"
          MATCHES=$(grep -R "$PATTERN" -n --exclude-dir=node_modules --exclude-dir=.turbo -- . || true)
          if [ -n "$MATCHES" ]; then
            echo "::error title=Unreplaced placeholders detected::%0A$MATCHES%0AReplace placeholder tokens before merging."
            exit 1
          fi
          [ -f .env.example ] || (echo "Missing .env.example" && exit 1)
          [ -f .env.local ] || cp .env.example .env.local

      - name: Lint GitHub Actions (actionlint)
        uses: reviewdog/action-actionlint@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          reporter: github-pr-review
          fail_on_error: 'false'
        continue-on-error: true

      - name: Lint
        run: pnpm --filter=web exec eslint . --max-warnings=0

      - name: Ensure workspace deps are linked
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm run typecheck

      - name: Build
        if: env.IS_INFRA_CHANGE != 'true'
        run: pnpm --filter=web run build

      - uses: actions/upload-artifact@v4
        if: success() && env.IS_INFRA_CHANGE != 'true'
        with:
          name: web-next-build
          path: apps/web/.next

      - name: Install Playwright browsers
        if: env.IS_INFRA_CHANGE != 'true'
        run: pnpm dlx playwright install --with-deps

      # If tests belong to the app, scope them; otherwise keep your root scripts.
      - name: Unit tests
        if: env.IS_INFRA_CHANGE != 'true'
        run: pnpm --filter=web run test:unit

      - name: Infra-only build complete
        if: env.IS_INFRA_CHANGE == 'true'
        run: |
          echo "🔧 Infrastructure-only PR detected"
          echo "✅ Skipped heavy tests (build, unit, E2E) as no application code changed"
          echo "📋 Only essential quality gates (lint, typecheck, doctor) were run"

  e2e:
    runs-on: ubuntu-latest
    needs: setup
    permissions:
      contents: read
    env:
      TURBO_TELEMETRY_DISABLED: '1'
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Detect Infrastructure Changes
        run: bash scripts/detect-infra-change.sh

      - name: No-op for infra-only PRs
        if: env.IS_INFRA_CHANGE == 'true'
        run: |
          echo "🔧 Infrastructure-only PR: Skipping E2E tests"
          echo "✅ No application code changes detected, E2E tests not needed"
          exit 0

      - name: Setup pnpm
        if: env.IS_INFRA_CHANGE != 'true'
        uses: pnpm/action-setup@v4
        with:
          version: 9.12.0
          run_install: false

      - uses: actions/setup-node@v5
        if: env.IS_INFRA_CHANGE != 'true'
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        if: env.IS_INFRA_CHANGE != 'true'
        run: pnpm install --frozen-lockfile

      - name: Build web app
        if: env.IS_INFRA_CHANGE != 'true'
        run: pnpm --filter=web run build

      - name: Install Playwright browsers
        if: env.IS_INFRA_CHANGE != 'true'
        run: pnpm dlx playwright install --with-deps

      - name: Run E2E tests
        if: env.IS_INFRA_CHANGE != 'true'
        run: pnpm run test:e2e:ci

  promote-gate:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: read
    steps:
      - name: Check promote label for bot PRs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          # Get PR head ref from environment (avoid direct interpolation)
          HEAD_REF="${PR_HEAD_REF}"
          echo "Head ref: $HEAD_REF"

          # Check if this is a bot branch
          if [[ "$HEAD_REF" == bots/claude/* ]]; then
            echo "🤖 Bot branch detected: $HEAD_REF"
            echo "🔍 Checking for 'promote' label requirement..."

            # Check if PR has promote label
            LABELS=$(gh api repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }} --jq '.labels[]?.name' || true)

            if echo "$LABELS" | grep -qi "^promote$"; then
              echo "✅ 'promote' label found - bot PR approved for merge"
              exit 0
            else
              echo "❌ Bot PR missing required 'promote' label"
              echo ""
              echo "🛡️ Security gate: Bot PRs require human approval via 'promote' label"
              echo "   A maintainer must review and add the 'promote' label before merge"
              echo ""
              echo "Available labels on this PR:"
              printf '%s\n' "$LABELS" | awk '{print "  - " $0}'
              exit 1
            fi
          else
            echo "👤 Human PR: $HEAD_REF"
            echo "✅ Promote gate not required for human-authored PRs"
            exit 0
          fi
